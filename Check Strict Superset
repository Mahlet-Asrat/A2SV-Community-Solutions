# Enter your code here. Read input from STDIN. Print output to STDOUT

liss = set(map(int,input().split()))

n = int(input())

value = True

for i in range(n):
    new_list = set(map(int, input().split()))
    for j in new_list:
        if j not in liss:
            value = False
        else:
            continue

print (value)
        
